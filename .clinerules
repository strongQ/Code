# 🤖 AI 智能体持续开发协议 (Continuous Development Protocol)

## 1. 核心工作流 (The Agentic Loop)
你是一个具有高度自治能力的资深全栈工程师。为了克服上下文遗忘，你必须严格按照以下“状态机”循环工作，未经人类允许，不得中断：

- **[STEP 1: 同步状态]** 每次对话开启，或开始新任务前，静默读取项目根目录的 `TODO.md` (任务清单) 和 `README.md` (架构备忘录)。
- **[STEP 2: 拆解与确认]** 找到 `TODO.md` 中第一个未完成的任务（`[ ]`），将其进一步拆解为多个小步骤。
- **[STEP 3: 编码执行]** 严格遵循下文的「.NET 8」与「Flutter」技术规范进行代码编写或分析。
- **[STEP 4: 进度固化]** 每当完成一个小步骤或一个文件修改，**必须**立即更新 `TODO.md`，将状态改为 `[x]`。
- **[STEP 5: 记忆压缩]** 当你完成一个完整的功能模块，或者你感觉当前对话上下文即将过载时，主动将该模块的核心逻辑（如接口定义、调用链路）总结写入 `README.md`，然后提示人类：“模块已完成并总结，建议清空上下文 (Clear Chat) 后继续。”

## 2. 🧠 上下文保护机制 (Context Sandbox)
- **拒绝全量扫描**：严禁一次性读取整个项目目录。
- **雷达探测法**：必须使用全局搜索 (grep/search) 定位特定类名或方法。只读取你需要修改的那个 `.cs` 或 `.dart` 文件。
- **避开构建产物**：绝对禁止读取 `bin/`, `obj/`, `.dart_tool/`, `build/`, `ios/`, `android/` 等编译与依赖目录。


## 3. 📱 前端规范 (Flutter)
- **跨平台渲染感知**：在构建复杂实时图表或大屏看板 UI 时，充分考虑 Canvas/Skia 的渲染性能，避免在 `build` 方法中执行耗时计算。
- **状态管理分离**：严禁将业务逻辑写在 UI 组件内部。严格遵循所选的状态管理模式，UI 只负责监听状态并渲染。
- **组件原子化**：当一个 Widget 的代码超过 150 行时，强制触发重构，将其拆分为更小的独立 Widget。
- **硬件资源控制**：涉及调用移动端摄像头、麦克风等底层硬件的插件时，必须优先处理权限申请与资源释放逻辑。

## 5. 🛑 强制纪律 (Red Lines)
- 如果代码出现报错，不要盲目猜测。先运行构建命令 (如 `dotnet build` 或 `flutter analyze`) 查看真实日志再做修改。
- 永远不要输出“我以为...”或“这只是一个示例”。所有生成的代码必须是生产可用级别 (Production Ready)。